[build]
builder = "nixpacks"

[deploy]
startCommand = "gunicorn app:app --bind 0.0.0.0:$PORT --workers 1 --threads 4 --timeout 1200"
healthcheckPath = "/health"
healthcheckTimeout = 60
restartPolicyType = "on_failure"
